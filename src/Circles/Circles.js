import React from 'react';
import "./Circles.css";

const Circles = (props) => {

        return (
                <div className="circle" onClick={props.click}>

                </div>
        )
    }


export default Circles;